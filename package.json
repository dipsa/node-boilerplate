{
  "name": "node-boilerplate",
  "version": "1.0.0",
  "description": "Node JS Boilerplate for AWS Lambda",
  "main": "dist/index.js",
  "dependencies": {
    "jest-cli": "^19.0.2"
  },
  "devDependencies": {
    "babel-cli": "^6.24.0",
    "babel-preset-latest": "^6.24.0",
    "gulp": "^3.9.1",
    "gulp-change": "^1.0.0",
    "gulp-install": "^1.1.0",
    "gulp-zip": "^4.0.0",
    "jest": "^19.0.2",
    "watch": "^1.0.2",
    "yargs": "^7.0.2"
  },
  "scripts": {
    "build": "babel src -d build",
    "dev": "watch 'npm run build' src",
    "test": "jest",
    "test:watch": "npm test -- --watch",
    "deploy": "babel src -d build && gulp && gulp change-metadata", "__comment": "example",
    "deploy-src": "babel src -d $GOLEM_LAMBDA_DEST",
    "deploy-artifact": "babel src -d build && gulp && gulp copy-artifact --D $GOLEM_LAMBDA_DEST"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dipsa/node-boilerplate.git"
  },
  "keywords": [
    "nodejs",
    "boilerplate",
    "aws",
    "lambda"
  ],
  "author": "pathumathukorala",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/dipsa/node-boilerplate/issues"
  },
  "homepage": "https://github.com/dipsa/node-boilerplate#readme",
  "babel": {
    "presets": [
      "latest"
    ]
  },
  "jest": {
    "testEnvironment": "node"
  }
}
